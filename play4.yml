---
- name: demonstrate file module
  hosts: localhost
  become: true
  tasks:
    - name: Create an empty file
      ansible.builtin.file:
        path: /etc/new_ansi.txt
        state: touch
        owner: root
        group: ansible
        mode: '0640'
...
#End
